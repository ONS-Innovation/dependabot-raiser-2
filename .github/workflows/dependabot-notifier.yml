name: Push Dependabot PRs

permissions:
  contents: read
  issues: write
  pull-requests: write

on:
  push:
    branches: ["**"]

jobs:
  create-dependabot-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Create issue
        id: create_issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_URL="https://github.com/${{ github.repository }}/commit/${{ github.sha }}"
          CREATED_URL=$(gh issue create \
            --title "Dependabot PR in ${{ github.repository }}" \
            --body "@joepercival Dependabot PR detected: \n\nCommit: $ISSUE_URL")